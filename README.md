# Encrypted research matrix

Authenticated private Python workload. The decryption key is supplied separately in the job environment. Code, frozen experiment data and the execution plan are encrypted. Credentials, paper drafts, previous results and model weights are excluded.
