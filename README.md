# Encrypted model preflight

Authenticated private Python workload. The decryption key is provided separately through the job environment. This repository contains no plaintext experiment code, datasets, checkpoints or credentials.
