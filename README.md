# Encrypted snapshot archival

Generic CPU utility for uploading authenticated encrypted snapshots to a user-selected Hugging Face repository. It verifies an anonymous download and every archived file before writing a receipt. Source files are retained. Credentials and encryption keys are supplied only in the job environment. No research code, dataset, checkpoint, or credential is included in this repository branch.
